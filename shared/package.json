{
  "name": "@controltower/shared",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Schemas and types shared by the Control Tower server and UI.",
  "license": "Apache-2.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'shared is consumed as source'",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^4.6.5"
  }
}
